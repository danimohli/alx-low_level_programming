This projct is about library in C propramming.
there are two types:
	. shared(dynamic) library
	. static library

This project is about Static library containing all the functions found in the main.h(header) file.
